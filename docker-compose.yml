services:
  ml-project:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    command: python -m pytest -q

  api:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: python -m uvicorn src.app:app --host 0.0.0.0 --port 8000
